<%= render 'layouts/error', obj: @detail %>

<div class="card text-xs-center">
  <div class="card-header card-primary text-white">現在の合計金額</div>
  <div class="card-block"><h4><%= @amount.to_s(:delimited) %>円</h4></div>
</div>

<br>

<div class="card">
  <div class="card-header card-primary text-white text-xs-center">明細を入力してください</div>

  <%= render 'form' %>

</div>
