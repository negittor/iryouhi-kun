<div>
    <%= form_for(@user) do |f| %>

      <div class="form-group row">
        <%= f.label "名前",id: "name", class: "col-xs-2 col-form-label" %>
        <%= f.text_field :name, class: "col-xs-8"%>
        <div class="col-xs-2">
          <%= f.submit "OK", class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
</div>
